# Renderer + Local Playhead - Implementation Tasks

> Auto-generated by `/impl-feature`. Feature spec: [features/renderer-playhead.md](./renderer-playhead.md)

<!-- PARTIAL STATE NOTICE -->
<!-- Partial implementations between sessions are EXPECTED and CORRECT. -->
<!-- If context is running low, update the CURRENT task's progress, then stop. -->
<!-- Do NOT rush to completion. Saving state IS the correct action. -->

**Session State:** complete
**Last Updated:** 2026-06-26

## Feature Summary

The foundation of BandAid: a single Svelte alphaTab renderer component, a local playhead
(alphaTab's player as the M1 clock + a pure `projectBar` reconciliation function), and a
session store holding `{currentSongId, transport}` behind a `SessionStore` interface so the
M2 CRDT impl drops in unchanged. Stack: Svelte + TypeScript + Vite; alphaTab assets +
soundfont bundled locally.

## Task Overview
| # | Task | Status | Acceptance Criteria |
|---|------|--------|---------------------|
| 1 | Scaffold + local assets | done | AC-2 |
| 2 | Session store + playhead math | done | AC-4, AC-5, AC-6 |
| 3 | alphaTab renderer component | done | AC-1, AC-7 |
| 4 | Playhead wiring + integration | current | AC-3, AC-8 |

**Progress:** 6/8 acceptance criteria complete (AC-3 partial, AC-8 web-only)

## Verified This Session (headless browse + screenshot)
- Renders Big John McNeil in guitar tab with bar numbers, title, tuning (AC-1). ✓
- All assets serve locally, no CDN: page, tune, Bravura font, sonivox soundfont 200s (AC-2). ✓
- Part selector re-renders per track — Fiddle/Guitar/Uke/Bass (AC-7). ✓
- 17 unit tests pass: projectBar math + store + fake-remote-writer (AC-4/5/6). ✓
- No console errors; clean build (vite build) + typecheck (svelte-check). ✓
- **Open:** playback cursor advancement (AC-3) not confirmable in headless — AudioContext
  won't resume without a real user gesture/audio device, and alphaTab ties the cursor to
  the running synth. Needs confirmation in a real browser. AC-8 mobile device test pending.

---
## Task 1: Scaffold + local assets
**Status:** current
**Expected Files:** package.json, vite.config.ts, tsconfig.json, index.html, src/main.ts, src/App.svelte, static/ (alphaTab fonts/worker + soundfont)
### Plan
- `git init` the project so the build is versioned from commit one.
- Scaffold Svelte + TS + Vite.
- Add alphaTab; bundle its fonts/worker + a small soundfont locally (no CDN).
- Verify dev server runs and assets load locally.
### What's Done
- (nothing yet)
### What's Remaining
- All of the above.
### Context for Next Session
- Stack decided: Svelte + TS + Vite (ADR-003 pending). Assets bundled locally (AC-2/NFR-1).

---
## Task 2: Session store + playhead math
**Status:** pending
### Plan
- `SessionState`/`Transport` types; `SessionStore` interface; local Svelte-store impl.
- `projectBar(transport, now)` pure function (meter-aware bars↔beats).
- Unit tests incl. a "fake remote writer" driving the store the way M2's CRDT will (AC-5).

---
## Task 3: alphaTab renderer component
**Status:** pending
### Plan
- Svelte component wrapping alphaTab (sole code touching its API).
- Load a sample multipart MusicXML; render notation + tab; `soloPart` per-track.

---
## Task 4: Playhead wiring + integration
**Status:** pending
### Plan
- `getPositionBar`/`seekToBar`/`onPosition` via alphaTab beat callbacks.
- Init cursor position from `projectBar`; mount component in App; minimal play/seek to make
  the cursor observable; verify responsive on web.
